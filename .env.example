# アプリケーションの環境変数例

# === 必須 ===
# Gemini API キー（互換のため GOOGLE_API_KEY でも可）
GEMINI_API_KEY=your_gemini_api_key_here
# セッション/CSRF 署名に使用する秘密鍵
SECRET_KEY=replace_with_a_random_secret

# === 本番必須 ===
# SQLAlchemy が理解できる接続文字列を指定（本番では未設定だと起動エラー）
# 例: Cloud Run + Cloud SQL (MySQL/PyMySQL, Unixソケット)
# DATABASE_URL=mysql+pymysql://<USER>:<PASS>@/<DB_NAME>?unix_socket=/cloudsql/<INSTANCE_CONNECTION_NAME>
# 例: ローカル SQLite
# DATABASE_URL=sqlite:///app.db

# DATABASE_URL を使わない場合は、以下の環境変数から MySQL 接続URLが自動構築されます。
# DB_USER=your_db_user
# DB_PASSWORD=your_db_password
# DB_NAME=your_db_name
# INSTANCE_CONNECTION_NAME=your-gcp-project:region:instance
# 任意: TCP接続用 (Cloud Run 以外のローカル/MySQL開発向け)
# DB_HOST=127.0.0.1
# DB_PORT=3306
# 任意: 明示的なソケットパスを使う場合
# DB_SOCKET=/cloudsql/your-gcp-project:region:instance

# === 任意（動作に応じて設定） ===
# 開発/本番/検証の切り替え（production 以外は開発扱い）
# 例: development / staging / production
APP_ENV=development
# デバッグ有効化（APP_ENV=production の場合は常に無効）
APP_DEBUG=true
# 互換のため Flask 標準のデバッグ指定も参照されます
# FLASK_DEBUG=1

# チャット画像の保存先 (local / gcs)
# 検証・開発環境は local を推奨（ローカル保存）
# CHAT_IMAGE_STORAGE=local
# local の保存先ディレクトリ（instance 配下の相対パスを推奨）
# CHAT_IMAGE_DIR=chat_images
# gcs の場合はバケット名を指定
# CHAT_IMAGE_BUCKET=your-gcs-bucket

# 初期ユーザー作成用（初回起動時の管理ユーザー）
# INITIAL_USER_USERNAME=admin
# INITIAL_USER_EMAIL=admin@example.com
# INITIAL_USER_PASSWORD=change-me

# Gemini モデル指定（未指定の場合はデフォルト値を使用）
# GEMINI_IMAGE_MODEL=gemini-3-pro-image-preview
# GEMINI_TEXT_MODEL=gemini-3-pro-preview

# 画像アップロード制限（ピクセル数）
# MAX_IMAGE_WIDTH=8192
# MAX_IMAGE_HEIGHT=8192
# MAX_IMAGE_PIXELS=67108864

# アップロードサイズ制限（バイト）
# MAX_CONTENT_LENGTH=33554432
# MAX_FORM_MEMORY_SIZE=33554432
